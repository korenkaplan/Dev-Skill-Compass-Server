{
    "builds": [{
        "src": "django_server/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "django_server/wsgi.py"
        }
    ]
}